import React from 'react';

import { InfoSection, PageWrapper } from 'components';

const Contact = () => (
  <PageWrapper
    left={<div>{/* TODO: Add contact logic */}</div>}
    right={<InfoSection />}
  />
);

export default Contact;
